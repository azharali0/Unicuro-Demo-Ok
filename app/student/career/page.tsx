import { requireRole } from "@/lib/session";
import { getCareerDashboard } from "@/lib/careerIntelligenceEngine";
export default async function Page(){const u=await requireRole(["STUDENT","MERCHANT"]);const d=await getCareerDashboard(u.id);return <main className="min-h-screen bg-white p-6"><section className="mx-auto max-w-7xl"><h1 className="text-5xl font-black">Career Centre</h1><p className="mt-4">Target role: {d.profile.targetRole||"Not set"}</p><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{d.opportunities.map(o=><article key={o.id} className="rounded-3xl border p-6"><h2 className="text-2xl font-black">{o.title}</h2><p className="mt-2">{o.provider}</p>{o.applicationUrl&&<a href={o.applicationUrl} className="mt-4 inline-flex font-black text-indigo-600">Apply</a>}</article>)}</div></section></main>}
