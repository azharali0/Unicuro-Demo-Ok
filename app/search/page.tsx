"use client";
import { useState } from "react";
export default function Page(){ const [q,setQ]=useState(""); const [results,setResults]=useState<any[]>([]); async function run(){ const r=await fetch(`/api/search?q=${encodeURIComponent(q)}`); const j=await r.json(); setResults(j.results||[]);} return <main className="min-h-screen bg-white p-6"><section className="mx-auto max-w-5xl"><h1 className="text-5xl font-black">Global Search</h1><div className="mt-6 flex gap-3"><input value={q} onChange={e=>setQ(e.target.value)} className="flex-1 rounded-2xl border p-4"/><button onClick={run} className="rounded-2xl bg-indigo-600 px-6 font-black">Search</button></div><div className="mt-6 grid gap-3">{results.map(r=><a key={r.id} href={r.route} className="rounded-2xl border bg-white p-4"><b>{r.title}</b><p>{r.summary}</p></a>)}</div></section></main>}
