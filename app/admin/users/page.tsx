import { requireRole } from "@/lib/session";
import { prisma } from "@/lib/prisma";
export default async function Page(){ await requireRole(["ADMIN","SUPER_ADMIN"]); const [wallets,profiles,merchants]=await Promise.all([prisma.studentWallet.findMany({take:50}),prisma.studentCareerProfile.findMany({take:50}),prisma.merchantProfile.findMany({take:50}).catch(()=>[])]); return <main className="min-h-screen bg-white p-6"><section className="mx-auto max-w-6xl"><h1 className="text-5xl font-black">User Management</h1><div className="mt-6 grid gap-4 md:grid-cols-3"><article className="rounded-[24px] border border-slate-200 bg-white p-6 shadow-[0_12px_40px_rgba(15,23,42,.05)]">Wallet users: {wallets.length}</article><article className="rounded-[24px] border border-slate-200 bg-white p-6 shadow-[0_12px_40px_rgba(15,23,42,.05)]">Career profiles: {profiles.length}</article><article className="rounded-[24px] border border-slate-200 bg-white p-6 shadow-[0_12px_40px_rgba(15,23,42,.05)]">Merchants: {merchants.length}</article></div></section></main>}
