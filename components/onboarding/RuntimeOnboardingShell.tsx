import Link from "next/link";
import { getOnboardingState } from "@/lib/onboardingRuntimeEngine";
import { AppIcon } from "@/components/ui/AppIcon";
export async function RuntimeOnboardingShell({userId,children}:{userId:string;children:React.ReactNode}){const state=await getOnboardingState(userId);const steps=state.flow?.steps||[];return <main className="ui-page"><section className="ui-container"><div className="grid gap-5 lg:grid-cols-[260px_minmax(0,1fr)]"><aside className="rounded-[24px] border border-slate-200 bg-white p-4 lg:sticky lg:top-5 lg:h-[calc(100vh-2.5rem)]"><div className="rounded-2xl border border-indigo-100 bg-indigo-50 p-4"><span className="inline-flex h-10 w-10 items-center justify-center rounded-xl bg-white text-indigo-600"><AppIcon name="spark" className="h-5 w-5"/></span><p className="mt-4 text-xs font-extrabold uppercase tracking-[.18em] text-indigo-600">Account setup</p><h1 className="mt-2 text-xl font-black">Personalise your experience</h1><p className="mt-2 text-xs leading-5 text-slate-600">Your answers securely configure your dashboard, learning tools and preferences.</p></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-indigo-600" style={{width:`${state.completionPercent}%`}}/></div><div className="mt-2 flex items-center justify-between text-xs font-bold text-slate-500"><span>Progress</span><span>{state.completionPercent}%</span></div><nav className="mt-5 grid gap-1.5">{steps.map(step=>{const complete=state.progress.some(p=>p.stepId===step.id&&p.status==="COMPLETED");const current=state.currentStep?.id===step.id;return <Link key={step.id} href={step.route} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-bold transition ${current?"bg-indigo-50 text-indigo-700":"text-slate-600 hover:bg-white"}`}><span className={`inline-flex h-7 w-7 items-center justify-center rounded-lg border text-xs ${complete?"border-green-200 bg-green-50 text-green-700":current?"border-indigo-200 bg-white text-indigo-700":"border-slate-200 bg-white text-slate-400"}`}>{complete?<AppIcon name="check" className="h-4 w-4"/>:step.sortOrder}</span><span>{step.title}</span></Link>})}</nav></aside><section className="min-w-0"><header className="mb-5 flex flex-wrap items-center justify-between gap-4 rounded-[24px] border border-slate-200 bg-white px-5 py-4"><div><p className="ui-eyebrow">UniCuro V2 onboarding</p><p className="mt-1 text-sm text-slate-500">Complete each step to unlock your personalised dashboard.</p></div><span className="ui-badge"><AppIcon name="shield" className="h-4 w-4"/>Saved securely</span></header>{children}</section></div></section></main>}
