"use client";
import { useState } from "react";
import Link from "next/link";
import { AuthVectorPanel } from "@/components/auth/AuthVectorPanel";
import { AppIcon } from "@/components/ui/AppIcon";
export function RegisterCard(){const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[role,setRole]=useState("STUDENT");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(){if(!name||!email||!password)return setError("Complete all required fields.");setLoading(true);setError("");try{const res=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,email,password,role})});const json=await res.json();if(!res.ok){setError(json.error||"Registration failed");setLoading(false);return;}window.location.href=json.redirectTo||"/onboarding"}catch{setError("Unable to connect. Please try again.");setLoading(false)}}return <main className="ui-page"><div className="ui-container flex min-h-screen items-center"><section className="grid w-full gap-5 lg:grid-cols-[1.05fr_.95fr]"><AuthVectorPanel/><div className="flex items-center justify-center rounded-[28px] border border-slate-200 bg-white p-6 sm:p-10"><div className="w-full max-w-md"><span className="ui-badge"><AppIcon name="plus" className="h-4 w-4"/>Create your workspace</span><h1 className="mt-5 text-4xl font-black tracking-[-0.04em]">Start your student journey</h1><p className="mt-3 leading-7 text-slate-600">Set up a secure account, then personalise your university experience.</p><div className="mt-7 grid gap-4"><label className="ui-label">Full name<input value={name} onChange={e=>setName(e.target.value)} className="ui-input" autoComplete="name"/></label><label className="ui-label">Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} className="ui-input" autoComplete="email"/></label><label className="ui-label">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="ui-input" autoComplete="new-password"/><span className="text-xs font-medium text-slate-500">Use at least 10 characters with a mix of letters and numbers.</span></label><label className="ui-label">Account type<select value={role} onChange={e=>setRole(e.target.value)} className="ui-input"><option value="STUDENT">Student</option><option value="MERCHANT">Student merchant</option></select></label></div>{error&&<p className="mt-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm font-bold text-red-700">{error}</p>}<button disabled={loading} onClick={submit} className="ui-button mt-6 w-full">{loading?"Creating account…":"Create account"}<AppIcon name="arrow" className="h-4 w-4"/></button><p className="mt-6 text-center text-sm text-slate-500">Already registered? <Link href="/login" className="font-extrabold text-indigo-600 hover:underline">Sign in</Link></p></div></div></section></div></main>}
