"use client";
import { useState } from "react";
import Link from "next/link";
import { AuthVectorPanel } from "@/components/auth/AuthVectorPanel";
import { AppIcon } from "@/components/ui/AppIcon";
type Role="student"|"admin"|"super-admin";
const data={student:{title:"Welcome back",subtitle:"Continue to your student workspace.",endpoint:"/api/auth/login/student"},admin:{title:"Administrator access",subtitle:"Sign in to manage platform operations.",endpoint:"/api/auth/login/admin"},"super-admin":{title:"Super administrator access",subtitle:"Secure governance and infrastructure access.",endpoint:"/api/auth/login/super-admin"}};
export function LoginRoleCard({role:initialRole="student"}:{role?:Role}){const[role,setRole]=useState<Role>(initialRole);const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[otp,setOtp]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);const cfg=data[role];async function submit(){setLoading(true);setError("");try{const next=new URLSearchParams(window.location.search).get("next")||"";const res=await fetch(cfg.endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password,otp,next})});const json=await res.json();if(!res.ok){setError(json.error||"Login failed");setLoading(false);return;}window.location.href=json.redirectTo;}catch{setError("Unable to connect. Please try again.");setLoading(false)}}return <main className="ui-page"><div className="ui-container flex min-h-screen items-center"><section className="grid w-full gap-5 lg:grid-cols-[1.05fr_.95fr]"><AuthVectorPanel/><div className="flex items-center justify-center rounded-[28px] border border-slate-200 bg-white p-6 sm:p-10"><div className="w-full max-w-md"><span className="ui-badge"><AppIcon name="shield" className="h-4 w-4"/>Protected sign in</span><h1 className="mt-5 text-4xl font-black tracking-[-0.04em]">{cfg.title}</h1><p className="mt-3 leading-7 text-slate-600">{cfg.subtitle}</p>{role!=="super-admin"&&<div className="mt-6 grid grid-cols-2 rounded-xl border border-slate-200 bg-white p-1"><button type="button" onClick={()=>setRole("student")} className={`rounded-lg px-4 py-2 text-sm font-bold ${role==="student"?"bg-white text-indigo-700 shadow-sm":"text-slate-500"}`}>Student</button><button type="button" onClick={()=>setRole("admin")} className={`rounded-lg px-4 py-2 text-sm font-bold ${role==="admin"?"bg-white text-indigo-700 shadow-sm":"text-slate-500"}`}>Admin</button></div>}<div className="mt-7 grid gap-4"><label className="ui-label">Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} className="ui-input" autoComplete="email"/></label><label className="ui-label">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="ui-input" autoComplete="current-password"/></label>{(role==="admin"||role==="super-admin")&&<label className="ui-label">Authenticator code<input inputMode="numeric" maxLength={6} value={otp} onChange={e=>setOtp(e.target.value.replace(/\D/g,""))} className="ui-input tracking-[.35em]"/></label>}</div><div className="mt-4 flex justify-end"><Link href="/login/forgot-password" className="text-sm font-bold text-indigo-600 hover:underline">Forgot password?</Link></div>{error&&<p className="mt-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm font-bold text-red-700">{error}</p>}<button disabled={loading} onClick={submit} className="ui-button mt-6 w-full">{loading?"Signing in…":"Continue"}<AppIcon name="arrow" className="h-4 w-4"/></button>{role==="student"&&<p className="mt-6 text-center text-sm text-slate-500">New here? <Link href="/register" className="font-extrabold text-indigo-600 hover:underline">Create an account</Link></p>}</div></div></section></div></main>}
